
import bcrypt from "bcrypt";

let now = performance.now();
// process.env.UV_THREADPOOL_SIZE = 1

bcrypt.hash("test",8, (err, hashed) => {
    let end = performance.now();
    console.log(end - now);
} )


bcrypt.hash("test",8, (err, hashed) => {
    let end = performance.now();
    console.log(end - now);
} )

bcrypt.hash("test",8, (err, hashed) => {
    let end = performance.now();
    console.log(end - now);
} )

bcrypt.hash("test",8, (err, hashed) => {
    let end = performance.now();
    console.log(end - now);
} )


bcrypt.hash("test",8, (err, hashed) => {
    let end = performance.now();
    console.log(end - now);
} )

bcrypt.hash("test",8, (err, hashed) => {
    let end = performance.now();
    console.log(end - now);
} )

bcrypt.hash("test",8, (err, hashed) => {
    let end = performance.now();
    console.log(end - now);
} )

bcrypt.hash("test",8, (err, hashed) => {
    let end = performance.now();
    console.log(end - now);
} )


